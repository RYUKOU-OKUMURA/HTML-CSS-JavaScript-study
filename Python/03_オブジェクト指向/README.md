# Pythonオブジェクト指向編

Pythonのオブジェクト指向プログラミング（OOP）について学びます。

## 📚 学習内容

### 1. [クラスとインスタンス](./01_classes.md)
- クラスの定義
- インスタンスの生成
- インスタンス変数とクラス変数
- メソッドの定義

### 2. [継承とポリモーフィズム](./02_inheritance.md)
- 継承の基本
- メソッドのオーバーライド
- super()の使い方
- 多重継承

### 3. [特殊メソッド](./03_special-methods.md)
- `__init__` - 初期化
- `__str__` と `__repr__` - 文字列表現
- `__len__`, `__getitem__` - シーケンス型の動作
- 演算子のオーバーロード

### 4. [プロパティとデコレータ](./04_properties.md)
- @property デコレータ
- getter と setter
- @classmethod と @staticmethod
- プライベート属性

## 🎯 学習目標

この章を完了すると、以下のことができるようになります:

- ✅ クラスを定義してインスタンスを生成できる
- ✅ 継承を使ってコードを再利用できる
- ✅ 特殊メソッドでPythonicなクラスを作れる
- ✅ デコレータを使って属性を管理できる

## 🏗️ オブジェクト指向の基本概念

### クラスとインスタンス

```python
class Dog:
    """犬クラス"""

    def __init__(self, name, age):
        self.name = name
        self.age = age

    def bark(self):
        return f"{self.name}がワンワン！"

# インスタンスの生成
my_dog = Dog("ポチ", 3)
print(my_dog.bark())  # ポチがワンワン！
```

### カプセル化

データと処理をまとめて、内部実装を隠蔽します。

```python
class BankAccount:
    def __init__(self, balance):
        self._balance = balance  # プライベート属性（慣習）

    def deposit(self, amount):
        if amount > 0:
            self._balance += amount

    def get_balance(self):
        return self._balance
```

### 継承

既存のクラスを拡張して新しいクラスを作ります。

```python
class Animal:
    def speak(self):
        pass

class Dog(Animal):
    def speak(self):
        return "ワンワン！"

class Cat(Animal):
    def speak(self):
        return "ニャー！"
```

### ポリモーフィズム

同じインターフェースで異なる動作を実現します。

```python
animals = [Dog("ポチ", 3), Cat("タマ", 2)]

for animal in animals:
    print(animal.speak())  # それぞれ異なる鳴き声
```

## 💡 いつクラスを使うべきか

### クラスを使う場合
- 関連するデータと処理をまとめたい
- 同じ構造のオブジェクトを複数作りたい
- 継承で機能を拡張したい

```python
# Good - 関連するデータと処理をクラスにまとめる
class User:
    def __init__(self, name, email):
        self.name = name
        self.email = email

    def send_email(self, message):
        print(f"{self.email}にメール送信: {message}")
```

### クラスを使わない方が良い場合
- 単純な処理だけの場合は関数で十分
- データだけをまとめたい場合は辞書やdataclassを検討

```python
# シンプルな処理 - 関数で十分
def calculate_area(width, height):
    return width * height

# データだけ - 辞書で十分
user = {"name": "太郎", "email": "taro@example.com"}
```

## 🎨 Pythonic なクラス設計

### dataclassを使う（Python 3.7+）

単純なデータクラスにはdataclassが便利です。

```python
from dataclasses import dataclass

@dataclass
class Point:
    x: float
    y: float

# 自動的に__init__, __repr__, __eq__が生成される
p1 = Point(1.0, 2.0)
p2 = Point(1.0, 2.0)
print(p1)       # Point(x=1.0, y=2.0)
print(p1 == p2) # True
```

### プロパティで属性を管理

```python
class Temperature:
    def __init__(self, celsius):
        self._celsius = celsius

    @property
    def celsius(self):
        return self._celsius

    @property
    def fahrenheit(self):
        return self._celsius * 9/5 + 32

temp = Temperature(25)
print(temp.celsius)     # 25
print(temp.fahrenheit)  # 77.0
```

## 📝 練習問題

### レベル1: 基礎

1. Bookクラスを作成してください（属性: title, author, year）
2. Rectangleクラスを作成し、面積を計算するメソッドを追加してください
3. Personクラスに自己紹介するメソッドを追加してください

### レベル2: 応用

1. Vehicleクラスを継承したCarとBikeクラスを作成してください
2. `__str__`と`__repr__`を実装したクラスを作成してください
3. @propertyを使ってCircleクラスの半径から直径と面積を計算してください

### レベル3: チャレンジ

1. ショッピングカートクラスを実装してください（商品の追加、削除、合計金額の計算）
2. スタック（Stack）データ構造をクラスで実装してください
3. イテレータを実装したカスタムクラスを作成してください

## 🔗 参考リンク

- [Python公式チュートリアル - クラス](https://docs.python.org/ja/3/tutorial/classes.html)
- [PEP 8 - クラスの命名規則](https://pep8-ja.readthedocs.io/)
- [dataclasses公式ドキュメント](https://docs.python.org/ja/3/library/dataclasses.html)

## ⏭️ 次のステップ

オブジェクト指向編を完了したら、[標準ライブラリ編](../04_標準ライブラリ/README.md)に進みましょう。
